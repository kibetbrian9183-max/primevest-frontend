# URL of your deployed Daraja backend (Render), no trailing slash.
VITE_API_BASE_URL=https://primevest-daraja-server.onrender.com

# Must match FRONTEND_API_KEY set on the backend.
# NOTE: this ends up in the browser bundle — see DEPLOY.md for what
# this key does and doesn't protect against.
VITE_API_KEY=
